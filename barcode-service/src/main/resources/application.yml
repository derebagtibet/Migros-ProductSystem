server:
  port: 8083

spring:
  application:
    name: barcode-service

services:
  category:
    url: ${SERVICES_CATEGORY_URL:http://localhost:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
